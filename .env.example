DATABASE_URL="postgresql://vos_user:PASSWORD@localhost:5433/vos_db?schema=public"
JWT_SECRET="change-me-in-env-only"
ENCRYPTION_KEY="64-hex-chars-32-bytes-generate-with-crypto.randomBytes(32).toString('hex')"
NEXT_PUBLIC_APP_NAME="منصة إدارة وتشغيل المتطوعين VOS"
PORT=3060
NODE_ENV=development

# اختياري — تفعيل إرسال البريد (مقابلة/تفعيل حساب/استعادة كلمة مرور)
SMTP_HOST=""
SMTP_PORT="465"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""
APP_BASE_URL="https://vos.ahlashabab.com"
